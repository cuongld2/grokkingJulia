tuple_example = ("Donald", 2, 4.5, "Thuy")
for iterate in tuple_example
    println(iterate)
end

example_string = "Are you happy now?"
for iterate_over_string in example_string
    println(iterate_over_string)
end